<Window x:Class="FileViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Viewer" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10">
            <TextBlock Text="Enter File Path:" Margin="0,0,0,5" FontWeight="Bold"/>
            <TextBox x:Name="FilePathTextBox" Width="400" Margin="0,0,0,5" Padding="5" BorderThickness="1" BorderBrush="DarkGray"/>
            <Button Content="View File" Click="ViewFile_Click" HorizontalAlignment="Center" Padding="10,5" Background="DodgerBlue" Foreground="White" BorderThickness="0"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <TextBlock x:Name="FileContentTextBlock" Margin="10" TextWrapping="Wrap"/>
        </ScrollViewer>
    </Grid>
</Window>
